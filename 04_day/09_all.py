# The all() function returns True only if all elements in an iterable are True.

# strings = ["Hello", "World", ""]
# print(all(strings))  # Output: False (because of the empty string)

# values = [1, 2, 3, 4]
# print(all(values))  # Output: True (all are non-zero)

